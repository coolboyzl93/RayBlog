// indexHeap.js
//索引堆解决的问题是，将一个数组变成堆以后，会打乱原数组的排列顺序。如何找到数组中原来的索引所对应的内容。
//索引堆是这样解决问题的，就是，不去将原数组的内容排列成堆，而是将原数组的索引变成一个堆。这样这个堆的根节点一定是
//原数组的最后一个元素，因为这个元素是最大的索引。这个索引指向的是最大的内容,注意，这里是我们要去指定的。

function indexMaxHeap(arr){

}
//当只是插入一个元素，此时这个元素就是根结点，那么这个元素的索引就是0,这个索引装的东西是原数组的索引号。
function insert(i,ele){
	var indexArr=[];
	arr[index[i]]
	var 

}